def getVersionCode() {  
    return (System.getenv("GITHUB_RUN_NUMBER") ?: "1") as Integer  
}  

android {  
    defaultConfig {  
        versionCode getVersionCode()  
        versionName "1.0.${getVersionCode()}"  
    }  
}